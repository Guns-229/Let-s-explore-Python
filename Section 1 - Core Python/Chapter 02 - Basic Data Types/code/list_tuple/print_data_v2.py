
def return_os_data():
    os_name, version, box_name = "React OS", "0.2.13", "My React OS"
    return os_name, version, box_name

os_name, version, box_name = return_os_data()
print(os_name, version, box_name)
